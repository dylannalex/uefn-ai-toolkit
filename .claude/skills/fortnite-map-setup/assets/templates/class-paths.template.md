# {{PROJECT_NAME}} — Class Paths

Fortnite device/Blueprint class paths discovered via `execute_python` against
the live editor for this project. Nothing here is hardcoded in `uefn-mcp` —
add a row the first time a path is found so it doesn't need rediscovering.

| Device / Blueprint | Class path | Notes |
| --- | --- | --- |
| | | |
